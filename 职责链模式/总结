职责链模式(责任链模式)：
核心：为请求创建了一个接受者对象的链
属于行为型模式

职责链模式通常每一个接受者都包含对另一个接受者的引用，
如果一个对象不能处理该请求，那么会穿给下一个接受者，以此类推

优点：
该模式对发送者和接受者进行解耦，提高系统灵活性
简化对象，对象不用知道链的结构
缺点：
性能会收到影响，特别是链特别长的时候
所以需要控制链中最大结点数量
调试不方便，递归调试逻辑复杂

应用场景：多级请求，各种审批流程等