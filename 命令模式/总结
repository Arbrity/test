命令模式：
支持可撤销命令
核心：将发起请求的对象和执行请求的对象解耦

调用者只要调用命令对象的execute方法即可让接受者工作，
从而不比知道具体的接受者对象是谁，是如何实现的，
命令对象负责让接受者执行请求的动作，起到了桥梁的作用

用该模式易设计一个命令对鞋，把命令对象放到列队，即可多线程的执行命令
容易实现对请求的撤销与重做

不足之处：可能会导致系统有过多的命令类，增加了系统复杂度

空命令也是一种设计模式，省去了判断是否为空的操作
